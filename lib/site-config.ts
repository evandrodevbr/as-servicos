/**
 * URL canônica de produção do site. Usada como fonte única de verdade por
 * `app/layout.tsx` (metadataBase), `app/sitemap.ts` e `app/robots.ts`.
 */
export const SITE_URL = 'https://asservicos.evandro.dev.br'
